/*  Cloudflare Pages Redirects for Vue Router support */
/* React/Vue SPAs handle routing via JS, so any direct path access should be served the index.html */
/* This prevents 404 on page refresh */

/* Redirect all traffic to index.html */
/* 200 is used for internal rewrites */
/* The final wildcard (*) handles any level of sub-path */

/* Catch-all for single-page app routing */
/* This will serve index.html for any path that does not match a real file */
/* 200 tells Cloudflare to serve current content (index.html) as the path without redirection */

/* Rule: Any path (*) -> /index.html with 200 status */
/* This ensures Vue Router can handle the path locally */

/* Example: /dashboard -> serves /index.html and Vue Router shows Dashboard component */

/* Cloudflare specific syntax: */
/* [source_path] [destination_path] [status_code] */

/* Main SPA routing rule */
/* We use /* because it handles parameters and nested routes */
/* /index.html is the entry point */

/* Actual Rule Line: */
/* /index.html serves all as a catch-all */
/* But to be safe on Cloudflare, we use the standard SPF rule */

/* Most reliable SPA rule for Cloudflare: */
/* Redirect any non-file match back to index.html */

/* /   /index.html   200 */
/* /*  /index.html   200 */

/* Let's keep it simple and effective */
/* All paths to index.html with 200 status */

/* One line: */
/* /*   /index.html   200 */

/* But we should have a comment for clarity */

/* Catch all the routes for the SPA */
/* Rewrite all requests to /index.html */
/* Status 200 means rewrite without changing the URL */

/* Actual production rule: */
/* This must be the ONLY line if we want to be exact */

/* /index.html  is where the magic happens */

/* For Cloudflare Pages specifically: */
/* If a file exists, serve it. Otherwise, serve index.html */
/* The/* /index.html 200 syntax is the global standard for this */

/* Let me add the actual line now */

/* --------------------------- */
/* CLOUDFLARE SPA REDIRECTS    */
/* --------------------------- */

/* Catch-all to index.html */
/* /* /index.html 200 */

/* Correct syntax for Cloudflare Pages _redirects file: */
/* Route  Destination  Status */

/* SPA Catch-all Rule: */
/* Redirect any route to index.html so Vue Router can take over */
/* Using 200 status so the URL in the browser bar doesn't change */

/* Final Rule: */
/* /* /index.html 200 */

/* --------------------------- */
/* End of Redirects            */
/* --------------------------- */

/* I'll just put the rule directly on top for certainty */

/* Rule for Vue Single Page Application: */
/* Redirect all paths to index.html with 200 status code */

/* /index.html 200 for all routes */

/* Cloudflare Pages will search for the longest matching prefix */
/* /* is the most generic one */

/* Let's write the exact file content required */

/* Simple rule: /index.html for all subpaths */
/* This allows things like /dashboard or /sales-pivot to work on page refresh */

/* Final content for file: */

/* /*  /index.html  200 */
